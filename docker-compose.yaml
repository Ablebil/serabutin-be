services:
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    container_name: serabutin-swagger-ui
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      SWAGGER_JSON: /openapi.yaml
      BASE_URL: /api-docs
    volumes:
      - ./docs/openapi.yaml:/openapi.yaml:ro
